.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.env
navigator.db
archives/
voices/
chroma/
registry.db

# SDK build output. sdk/src/generated/ is Fern's output -- regenerate it with
# `npx fern-api generate`, never commit it.
sdk/node_modules/
sdk/dist/
sdk/src/generated/
